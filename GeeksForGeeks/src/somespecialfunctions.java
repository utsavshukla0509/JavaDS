import java.util.BitSet;
import java.util.Stack;

public class somespecialfunctions {

	public static void main(String[] args) {

		// 1...System.out.println(Integer.toBinaryString(5)); convert decimal to binary
		// 2...System.out.println(Long.bitCount(5)); count the no of set bits(1's) in a
		// given no.
		// 3... static BitSet col = new BitSet(30); to get a bitset array
		// col.set(c, false); to set the bitset array 1 or 0
	
		
	}

}
